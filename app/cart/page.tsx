import { CartView } from "@/components/cart/cart-view"

export const metadata = {
  title: "Cart | Muse",
  description: "Review your custom art prints and proceed to checkout.",
}

export default function CartPage() {
  return <CartView />
}
